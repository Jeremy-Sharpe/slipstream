# Board

Claim a feature by writing your name and branch on its row and pushing that change to `main` before writing code. Rules in `CLAUDE.md`. Status is one of `unclaimed`, `in progress`, `blocked`, `done`.

Order matters. The foundation rows unblock everything else and should be claimed first.

## Foundation (blocks everyone)

| Slug | Feature | Folders | Owner | Branch | Status | Notes |
|---|---|---|---|---|---|---|
| `web-skeleton` | Next.js app, layout, the three surfaces as routes, mock data | `app/` | Jeremy | main | done | Prototype evolved into the production UI at `https://slipstream-hackathon.vercel.app`; live API reads coexist with explicitly labelled evaluation fallbacks. |
| `schema` | Supabase project, pgvector, migrations for the tables in PROJECT.md, seed script | `supabase/` | Jeremy's agent | feat/schema | done | SQL parser clean and hostile reviews completed. Only `20260912000000` is applied to the hosted project; eight later migrations are merged but unapplied, so email ingestion, scorecard, playbook, unified-ICP, delivery-ledger and campaign functions do not exist there. Needs `supabase db push` |
| `fixtures` | Twelve labelled call scripts as CRM history plus one voiced demo call, expected extraction and scorecard labels, validator, ElevenLabs generator | `fixtures/` | Anna | feat/fixtures | done | Scripts, labels, validator and tests merged; demo audio generated once the ElevenLabs key exists (`generate_audio.py --demo`) |
| `api-skeleton` | FastAPI app, settings, Supabase client, health route, CORS for the web origin, VPS deploy | `api/` | Jeremy's agent | feat/api-skeleton | done | Live at `https://slipstream-api.3-104-149-193.sslip.io`; 18 foundation tests and hostile review passed |
| `web-deploy` | Vercel project for the UI, env vars, production URL in README | Vercel, `README.md` | Anna + Jeremy's agent | main | done | Independent production deploy at `https://slipstream-hackathon.vercel.app`, connected to GitHub; VPS CORS verified for the exact origin |
| `web-vps-fallback` | Standalone same-origin presentation host when Vercel's free deployment quota is exhausted | `next.config.ts`, `deploy/web/`, VPS Caddy + systemd | Jeremy's agent | feat/web-vps-fallback | done | PRs #59-60 are live at `https://slipstream.3-104-149-193.sslip.io`; pitch polish through PR #69 is deployed as immutable web revision `a21e2ba`. Loopback Node port 3010, explicit API/schema proxy boundary, full production smoke and real Revenue DNA browser interactions all pass. Other people should use this URL for presenting and leave port 3010 reserved. |
| `web-split` | Move analysis and leads markup out of `app/page.tsx` into `app/analysis` and `app/leads`, nav into `components/shell/`, then break the conversations surface into components and data modules | `app/page.tsx`, `app/conversations/`, `components/`, `lib/` | | | unclaimed | Hour one unblocks Max's UI lane; see `docs/start-here.md` for the folder split |

## Lanes

| Slug | Feature | Folders | Owner | Branch | Status | Notes |
|---|---|---|---|---|---|---|
| `ingest` | Upload or pick a fixture call, Scribe batch transcription with diarisation, transcript stored | `api/app/routers/calls.py`, `api/app/services/transcribe.py` | Jeremy's agent | feat/ingest | done | 33 tests pass; hostile final review clean; fixture path works without keys and paid Scribe is guarded and bounded |
| `email-ingest` | Ingest inbound/outbound email threads, map participants into CRM-shaped records, and draft a grounded reply from thread history | `api/app/routers/emails.py`, `api/app/services/email.py` | Jeremy's agent | feat/email-ingest | done | Provider/mailbox scoped, atomic Supabase RPCs, safe recipient roles, versioned drafts, 114 combined API tests and database integration coverage; hostile final review clean Note (Anna, 13 Sep): `model-coverage` adds a model path for `draft_thread_reply`; the template stays as the fallback. |
| `extract` | Claude structured extraction to contacts, companies, deals, notes, tasks, activities, with confidence and transcript spans | `api/app/services/extract.py`, `api/app/prompts/extract-*.md`, `api/app/schemas/` | Jeremy's agent | feat/extract | done | 37 tests pass; all 13 fixtures run; Anthropic structured schema and evidence checks received a clean hostile review |
| `draft` | Follow-up email draft per call; approval is audited separately from delivery | `api/app/services/draft.py`, `api/app/routers/drafts.py` | Jeremy's agent | feat/draft | done | 57 combined API tests pass; review findings fixed; CRM upserts preserve existing values and approval is audited/idempotent |
| `scorecard` | Rubric document, LLM-as-judge scorecard per call, twelve-call labelled eval and script | `api/app/services/score.py`, `api/evals/` | Anna | feat/scorecard | done | Anna built the rubric, playbook and eleven-model bake-off; Jeremy's agent integrated and hardened evidence, bounds, concurrency, canonical-source persistence and eval accounting (148 API tests); DeepSeek V3.2 picked (see `api/evals/README.md`); deployed on the VPS at revision `4052036` Note (Anna, 14 Sep): production 502 on a lost call traced to the judge returning an evidence item with an empty quote, which strict validation rejected whole; blank evidence items are now dropped before validation (`fix/judge-blank-evidence`). |
| `icp` | Embed won-deal summaries, derive the ICP with evidence, generate the Origami brief | `api/app/services/icp.py`, `api/app/routers/icp.py` | Anna | feat/icp-leads | done | Runs end to end on the OpenRouter key alone (reasoning and embeddings fall back to OpenRouter when the native key is absent); Supabase store path untested because the migration is not yet applied to the project. Embeddings: text-embedding-3-small |
| `leads` | Origami search from the brief, job polling, rows to `leads`, similarity scoring against won deals | `api/app/services/origami.py`, `api/app/routers/leads.py` | Anna + Jeremy's agent | feat/icp-leads + feat/origami-v3-contract | done | Current v3 contract is implemented and hardened in PR #11; the `ORIGAMI_API_KEY` in the shared `.env` is still a placeholder, so a live credit-spending run remains unclaimed. Start at `count: 10` |
| `outreach` | Outreach draft per lead, approve | `api/app/services/outreach.py` | Anna | feat/icp-leads | done | Provider-agnostic lead-grounded drafting and audited approval landed at `a47a7fb`; PR #13 later separated approval from Resend delivery and made the exact draft immutable once armed. The live Leads UI exposes the flow with evaluation fallback. Other people should provide an Origami key only through deployment secrets and record the public demo video. |
| `live-extraction` | Extraction and the follow-up draft run on the configured reasoning model for every call, fixtures included, with a deterministic grounding pass; extraction eval over the 13 fixture labels | `api/app/services/extract.py`, `api/app/services/draft.py`, `api/app/routers/extractions.py`, `api/app/routers/drafts.py`, `api/app/prompts/follow-up-*.md`, `api/evals/run_extraction_eval.py` | Anna | feat/live-extraction | done | PR #5 merged the provider-backed schema path, deterministic verbatim grounding, model-written follow-up and thirteen-call eval runner. PR #22 preserved Anna's final seven-model results without merging stale code: Mistral Medium 3.1 led at 86.5% mean judged pass rate, 7.4 s and $0.0034/call, while two per-check gates remain openly failed. Other people should record the public demo video. |
| `web-wire-conversations` | Replace mock conversations with Supabase reads and API calls: transcript, extracted fields with approval, scorecard, draft | `app/` conversation views, `lib/` | Jeremy's agent | feat/web-wire-conversations | done | Max's Clay-style UI merged; live fixture ingest → evidence-backed CRM extraction → grounded draft → approval runs against production, with explicit labelled fallback and clean hostile review |
| `web-wire-emails` | Add email threads to the unified feed and wire real ingest, thread history, grounded reply draft and approval | `app/(app)/conversations/`, `components/conversations/email/`, `lib/api/`, `lib/data/` | Jeremy's agent | feat/web-wire-emails | done | Two labelled threads now run provider-scoped ingest → ordered history → grounded reply → exact draft approval in the unified UI; production smoke, lint, build and final hostile review clean |
| `web-wire-analysis` | Replace mock analysis and ICP data with live reads | `app/analysis` | Jeremy's agent | feat/web-wire-analysis | done | Intelligence loads validated stored ICP and Origami brief data from the deployed API; retained aggregate insights and exact-ID evidence mappings are explicitly labelled evaluation data; build and hostile review findings resolved |
| `web-wire-scorecards` | Load or generate per-call scorecards and derive an aggregate playbook through the API, with explicit provenance | conversation detail, `app/intelligence`, `lib/api/` | Jeremy's agent | feat/web-wire-scorecards | done | Explicit score/playbook actions, exact transcript and score-result revision pinning, evidence grounding, validated live lens and per-section provenance are live; 174 API tests, build and five hostile review rounds pass. Other people should record the demo video. |
| `playbook-store` | Persist revision-pinned aggregate playbooks and expose the latest provenanced cohort | `api/`, `supabase/migrations/`, `app/intelligence` | Jeremy's agent | feat/playbook-store | done | Immutable cohort hashes, atomic post-judge revision checks, newest-current fallback, delayed-write protection and Intelligence hydration are live at API revision `70d1cbc`; 176 tests, build and final hostile review clean. Other people should record the public demo video. |
| `web-wire-leads` | Replace mock leads with live Origami results, similarity score, outreach approve | `app/leads` | Jeremy's agent | feat/web-wire-leads | done | Validated live lead reads, bounded Origami polling, backend similarity, exact-draft outreach approval and labelled evaluation fallback wired; stale approvals return 409 under a shared per-lead lock; 151 API tests, repeated race test, build and hostile review pass |
| `coach-shell` | Fork Cheating Daddy into `coach/`, remove Gemini, connect to the API WebSocket, keep overlay and audio capture | `coach/` | Jeremy's agent | feat/coach-shell | done | GPL-preserving transparent Electron overlay; credential-free manual demo plus consented rep-microphone Scribe mode, strict IPC/permission boundaries, production protocol smoke, 9 tests, zero production audit findings, Linux `.deb` build, final hostile review clean. Note (Romain, 13 Sep): this desktop app is replaced in `coach/` by the session-based coach under `call-coach` |
| `coach-release` | Build unsigned coach installers for Linux, macOS and Windows in GitHub Actions | `.github/workflows/`, `coach/` | Jeremy's agent | feat/coach-release | done | Exact main revision `d876e5c` passed tests, native packaging and Node-24 artifact upload on all three hosted runners in Actions run `34694011356`; final hostile review clean. Other people should record the public demo video and should not add signing secrets to the repository. Note (Romain, 13 Sep): the workflow now builds the macOS installer for the replacement coach under `call-coach` |
| `coach-brain` | WebSocket endpoint: Scribe realtime in, rolling Claude suggestions out with deal context, hand recording to ingest on call end | `api/app/ws/coach.py`, `api/app/prompts/coach-*.md` | Jeremy's agent | feat/coach-brain | done | 79 API tests pass; final hostile review clean; authenticated Scribe tokens, resumable sessions, bounded model spend, grounded fallback, and canonical call persistence. Note (Anna, 12 Sep): two `test_coach.py` tests fail when `OPENAI_API_KEY` is set in the shell; the test setup should clear provider env vars like `conftest.py` does |
| `pitch` | `docs/pitch.md`: 400 to 700 spoken words plus a Q&A section, scored by evals F1 and F3 | `docs/pitch.md` | Jeremy's agent | feat/pitch | done | PR #58 leaves 499 short, stage-ready spoken words with no unexplained specialist terms; model judges score F1 at 6/6 and F3 at 6/6; questions split across all four teammates |
| `demo-script` | `docs/demo-script.md`: step by step against the live app with a fallback, scored by eval F2 | `docs/demo-script.md` | Jeremy's agent | feat/demo-script | done | Live API fallback exercised; model judge F2 now scores the flow 8/8 and the script explicitly rechecks that fictional `.example` leads render after search |
| `video` | 3 to 5 minute demo video on the live URL, per-feature walkthroughs | `docs/video/` | Jeremy's agent | feat/demo-video-v2 | done | PR #66 published the 3:48 Revenue DNA-first live-product walkthrough at release `demo-video-v2`. It visibly performs the non-mutating outcome shock and closes on the real paused zero-send campaign; H.264/AAC metadata, normalized narration, silence scan and eight sampled frames pass. The v1 asset remains recoverable. Other people should use v2 or replace it only with a rehearsed human-presented cut. |
| `docs-decisions` | Architecture decision record, consolidated model-evaluation record, README documentation index | `docs/decisions.md`, `docs/model-bakeoff.md`, `docs/architecture.md`, `README.md` | Anna | worktree-docs-decisions | done | Judge-facing: the T2, T3 and T4 evidence in one place |
| `submission` | README lines filled, `npm run evals:dry` green, form submitted before Monday 12:00pm | `README.md` | Jeremy's agent | feat/submission-readiness | in progress | Production URL, track and the 3:48 Revenue DNA-first fallback video are filled; 367 API tests and 52 smoke tests pass; all 15 final internal rubric scenarios also pass at 95/100. A teammate only needs to submit the external form, or optionally replace the fallback with a rehearsed human-presented cut first. |
| `ci` | Protect main with credential-free web, API, fixture and coach checks | `.github/workflows/` | Jeremy's agent | feat/ci | done | Exact main revision `d876e5c` passed all four clean-runner jobs without annotations in Actions run `34694007012`; action versions are immutable commit pins. Other people should record the public demo video and avoid adding provider secrets to CI. |
| `database-ci` | Apply all migrations and run pgTAP tests in ephemeral Supabase on pull requests | `.github/workflows/`, `supabase/` | Jeremy's agent | feat/database-ci | done | PR #8 applied all four migrations and passed all three pgTAP suites in ephemeral Supabase (run `34695291147`), catching and fixing the pre-existing pgcrypto schema bug. Other people should record the public demo video. |
| `unified-icp-evidence` | Derive ICPs from outcome-labelled CRM deals enriched by both call and email history without changing the fixture benchmark | `api/app/services/icp.py`, `api/app/services/icp_leads_store.py`, channel ingestion paths | Jeremy's agent | feat/unified-icp-evidence | done | PR #9 merged at `6e2cfc4`: private bounded CRM snapshot, immutable won-deal evidence, non-destructive memory mirroring and visible channel provenance. All 182 API tests, web build, five migrations and four pgTAP suites passed in run `34696878908`; seventh hostile review clean. Other people should record the public demo video. |
| `icp-evidence-inventory` | Show a credential-free live inventory of call/email evidence ready for the next ICP derivation | `api/app/routers/icp.py`, `components/intelligence/`, `lib/api/` | Jeremy's agent | feat/icp-evidence-inventory | done | PR #10 merged at `e5aa1a4`: the exact bounded ICP model cohort is readable without provider keys and visible with honest evidence/provider/provenance labels. All 184 API tests, web build, five migrations and four pgTAP suites passed in run `34697558211`; third hostile review clean. API deployed; Vercel refused the matching web deploy after the project exceeded 100 free deployments that day. Other people should record the public demo video. |
| `origami-v3-contract` | Align lead search and row ingestion with Origami's published v3 wire contract | `api/app/services/origami.py`, `api/app/services/leads.py`, `api/tests/` | Jeremy's agent | feat/origami-v3-contract | done | PR #11 merged at `c6230fb`: quality forwarding, typed cells, v3 envelopes, repeated-ID pagination, strict deadlines, retry semantics, truthful provenance and scoped provider-result bounds. All 201 API tests and five CI jobs passed in run `34699477775`; final hostile reviews were clean. API exact revision is live and Vercel passed. Other people should record the public demo video and provide a real Origami key only through deployment secrets. |
| `crm-webhook-sync` | Deliver approved, evidence-backed call extraction into an existing CRM through a signed provider-neutral webhook | `api/app/routers/crm.py`, `api/app/services/crm_webhook.py`, `api/app/core/config.py` | Jeremy's agent | feat/crm-webhook-sync | done | PR #12 merged at `7f0f360`: authenticated exact-payload retry identity, bounded CRM fields, no transcript export, corrected-payload delivery, fail-fast admission and honest ambiguous outcomes. All 218 API tests and five CI jobs passed in run `34700433859`; third hostile review clean. Exact API revision and OpenAPI route are live with the unconfigured integration failing safely. Other people should record the public demo video and install receiver secrets only in deployment configuration. |
| `email-delivery` | Separate approval from actual send and deliver approved follow-up and outreach drafts through a credential-gated provider | `api/app/routers/drafts.py`, `api/app/services/email_delivery.py`, `api/app/services/outreach.py`, UI approval labels | Jeremy's agent | feat/email-delivery | done | PR #13 merged at `4bd5000`: approval is truthfully unsent; authenticated Resend delivery uses exact-content identity, a durable pre-send ledger, 23h55 ambiguous-retry cutoff and atomic draft/lead/receipt/audit completion. All 244 API tests, six migrations, five pgTAP suites and every CI job passed in run `34702933115`; fifth hostile review clean after 3 high, 12 medium and 2 low findings were fixed. API and Vercel are live; `/ready` truthfully reports `email_delivery: false` until deployment-only Supabase and Resend credentials exist. Other people should record the public demo video and apply the five pending hosted migrations when credentials are available. |
| `batch-delivery` | Execute an explicit bounded set of approved follow-up and outreach drafts for a campaign or scheduler, with an itemized outcome for every draft | `api/app/routers/deliveries.py`, `api/tests/test_batch_delivery.py`, `docs/` | Jeremy's agent | feat/batch-delivery | done | PR #14 merged with explicit-ID enrollment, ordered partial outcomes, bounded admission, disconnect/cancellation handling and a backwards-compatible durable submission ledger. All 277 API tests and seven GitHub checks passed; API revision `12b7fba` is live. Other people should keep the browser token-free and use this server-side primitive from Railway, Marcel, or another trusted scheduler. |
| `campaign-orchestration` | Persist explicit outreach campaign membership, let a trusted Railway/Marcel scheduler run due campaigns, and expose resumable itemized status without a browser delivery token | `api/app/routers/campaigns.py`, `api/app/services/campaigns.py`, `supabase/migrations/`, `app/leads/` | Jeremy's agent | feat/campaign-orchestration | done | PR #15 merged at `3712890`: exact approved-draft enrollment, immutable retry identity, bounded leased execution, durable per-item outcomes, retry/reconcile continuation and browser-safe status reads. All 287 API tests, eight migrations, six pgTAP suites and seven CI checks passed after hostile review; exact API revision is live. Other people should install Supabase, Resend and scheduler secrets only in deployment configuration and record the public demo video. |
| `web-wire-campaign-status` | Show live execution campaigns and per-item delivery state in the existing campaigns surface without exposing scheduler credentials or mislabelling the sequence-design prototype | `components/campaigns/`, `lib/api/` | Jeremy's agent | feat/web-wire-campaign-status | done | PR #16 merged at `d6db128`: strict live status hydration, expandable per-draft outcomes and receipts, truthful empty/error states, race-safe refresh and accessible announcements. All five repository CI jobs passed in run `34709284276` and hostile-review findings were fixed. Production deployment `dpl_BYqy6vsD3jnBTxRJ8ZRiJfHW9wEX` is healthy and the canonical Campaigns page exposes “Delivery execution”. Other people should record the public demo video. |
| `campaign-controls` | Let trusted operators pause and resume scheduled campaigns without changing membership or exposing controls to an unauthenticated browser | `api/app/routers/campaigns.py`, `api/app/services/campaigns.py`, `supabase/migrations/`, `docs/` | Jeremy's agent | feat/campaign-controls | done | PR #17 merged at `e92c87b`: authenticated idempotent pause/resume, in-flight conflict fencing and preserved pending schedule in memory and Postgres. All 289 API tests, nine migrations, seven pgTAP suites and seven CI checks passed; hostile review clean and exact API revision live. Other people should install provider secrets only in deployment configuration and record the public demo video. |
| `scheduler-worker` | Package the trusted campaign runner as a one-shot Railway cron container with strict timeouts and secret-only configuration | `scheduler/`, `railway.json`, `docs/` | Jeremy's agent | feat/scheduler-worker | done | PR #18 merged at `1a75a1b`: one-shot standard-library container, five-minute Railway cron config, strict URL/header/response validation, redirect rejection, hard wall-clock and SIGTERM deadlines, PII-free logs and no in-process retries. Seven worker tests plus all six other CI jobs passed; three hostile review rounds resolved. Railway deployment awaits account login only. Other people should add secrets through Railway variables and record the public demo video. |
| `campaign-demo-story` | Put safe campaign automation into the spoken pitch, timed demo, README and judge Q&A without claiming the keyless deployment sent email | `docs/demo-script.md`, `docs/pitch.md`, `README.md` | Jeremy's agent | feat/campaign-demo-story | done | PR #19 merged at `fbe3342`: 602-word pitch, timed conditional campaign beat, duplicate-send Q&A and truthful README architecture. All six CI jobs passed; deterministic submission checks now fail only on the human-owned demo video URL. Other people should record and publish that video, then rerun the final checklist. |
| `production-smoke` | Add one credential-free, non-mutating command that verifies the public UI, campaign surface, API revision/schema and protected scheduler boundary before recording or submission | `scripts/`, `package.json`, `docs/submission-checklist.md` | Jeremy's agent | feat/production-smoke | done | PR #20 merged at `cdf2649`: HTTPS-only bounded checks cover the live Campaigns marker, exact API revision, public campaign reads, all campaign HTTP operations and rejected unauthenticated controls. Three offline contract tests and all six CI jobs passed in run `34711088144`; the real production check also passes. Other people should record and publish the public demo video, then run this gate with the final main SHA. |
| `demo-campaign-seed` | Recreate one safely paused synthetic campaign after a memory-backed API restart, with hard guards against real storage or email delivery | `scripts/`, `docs/demo-script.md`, `docs/submission-checklist.md` | Jeremy's agent | feat/demo-campaign-seed | done | PR #21 merged at `fb1cc4b`: an idempotent HTTPS-only command recreates the fixture call, grounded approved draft and year-2099 paused campaign, while refusing durable storage or configured email delivery. The PR #52 follow-up discovered and repaired the missing command-line invocation; the documented npm command now actually executes and has a fail-fast regression test. Production shows one queued, zero sent and zero attempts. Other people should submit the external form and must not resume this campaign. |
| `extraction-eval-evidence` | Preserve Anna's completed seven-model extraction bake-off on current main without merging the stale implementation changes from PR #7 | `api/evals/`, `README.md`, `docs/` | Jeremy's agent | feat/extraction-eval-evidence | done | PR #22 merged at `61cffcb`: the comparison and missing OpenRouter per-call reports are attributed to Anna, distinguish unknown OpenAI cost from zero, preserve all failed gates and exclude obsolete application code. All six CI jobs passed in run `34712070620`; superseded PR #7 was closed with a handoff comment. Other people should record the public demo video. |
| `evidence-sequence-repair` | Accept a model quote with an omitted segment index long enough for deterministic grounding to locate it, while keeping canonical extraction results strictly indexed | `api/app/schemas/extraction.py`, `api/app/services/extract.py`, `api/tests/` | Jeremy's agent | feat/evidence-sequence-repair | done | PR #23 merged at `29fd824`: raw model spans with a missing index now reach deterministic quote grounding, while canonical results still reject every unindexed transcript span. All 292 API tests and all six repository CI jobs passed in run `34712518252`; the historical bake-off remains unchanged until a credentialed rerun. Other people should record the public demo video. |
| `eval-live-route-evidence` | Give rubric judges bounded direct evidence from every public product surface and robustly parse a final JSON verdict whose evidence strings contain braces | `evals/lib/`, `scripts/eval-evidence.test.mjs`, `docs/` | Jeremy's agent | feat/eval-live-route-evidence | done | PR #25 merged at `a758189`: every judge gets bounded HTTP-200 evidence from five public routes, and brace-containing or pretty final verdicts parse reliably. Ten smoke tests pass; targeted T1 rose to 8/10 and T3 passed. All six repository jobs passed in run `34714623752`. Other people should submit the external form. |
| `eval-score-reaffirmation` | Preserve an explicit criterion score when a late background-review notification replaces the judge's requested JSON footer | `evals/lib/judge.mjs`, parser tests, coordination docs | Jeremy's agent | fix/eval-score-stands-parser | done | The first full current-state suite passed every substantive criterion, but T2 broke only because its final notification said “score stands at 8/8” without JSON. The bounded fallback accepts that exact verdict form, rejects hypothetical score language, and a fresh T2 run passes at 8/8. All 51 smoke tests pass. Other people should wait for the final aggregate before quoting the full suite. |
| `delivery-service-boundary` | Separate delivery lifecycle business rules from FastAPI transport without changing the endpoint contract | `api/app/services/deliveries.py`, `api/app/routers/deliveries.py`, API tests, architecture contract | Jeremy's agent | refactor/delivery-service-boundary | in progress | The 1,097-line router is now a 33-line transport adapter over one shared delivery state machine. All 367 API tests pass. Other people should keep claims, leases, rollback, completion and batch orchestration in the service rather than the router. |
| `campaign-loading-boundary` | Replace the route fallback's full client campaign workspace with a static skeleton so Suspense does not mount a duplicate live API reader | `app/(app)/campaigns/loading.tsx`, `components/campaigns/` | Jeremy's agent | feat/campaign-loading-boundary | done | PR #26 merged at `3fdc086`: the Suspense fallback is a static accessible skeleton and cannot mount hooks or a second campaign API reader. Eleven smoke tests, lint, a 28-route build and all six repository CI jobs passed in run `34714806940`; local production HTML contains exactly one execution panel. Other people should submit the external form. |
| `demo-evidence-parity` | Align the generated demo-call UI with its canonical voiced fixture, include the scripted Maya opening and live API campaign state in judge evidence, and tolerate a judge's explicit final score when it omits JSON | `lib/data/calls.ts`, `evals/lib/`, `docs/demo-script.md`, `scripts/` | Jeremy's agent | feat/demo-evidence-parity | done | PR #27 merged at `0875dde`: call 13 is canonically Donnie/Jordan across fixture, UI and production; the Maya opening and live campaign API are direct judge evidence; F2, 15 smoke tests, lint, build and all six repository jobs pass. T1 scored 6/10 after the evidence exposed the real keyless memory deployment; no usable Supabase/model/delivery secrets are available unattended. Other people should submit the form and supply credentials only through an approved secret channel. |
| `self-hosted-supabase` | Run the reviewed Supabase schema durably on the existing VPS when managed-project credentials are unavailable, with reproducible startup, TLS-only REST access and verified API persistence | `deploy/supabase/`, `supabase/`, `docs/`, VPS runtime | Jeremy's agent | infra/self-hosted-supabase | blocked | PR #28 was reverted at `a55d064` after production verification showed the CLI local stack binds database services to every interface and uses shared defaults. The fail-closed path ran; all containers, volumes, generated credentials and service wiring were removed, ports were verified closed, and the healthy API was restored. Use approved managed credentials or a hardened self-hosted stack, not the CLI local runtime. |
| `production-primary-loop-smoke` | Exercise the canonical call through production ingestion, CRM extraction, grounded drafting and audited unsent approval, then expose bounded read-only runtime evidence | `scripts/smoke-production.mjs`, `scripts/smoke-production.test.mjs`, `evals/lib/repo.mjs`, `components/conversations/detail/`, `docs/` | Jeremy's agent | feat/production-primary-loop-smoke | done | PR #29 merged at `e98e76a`; production exact-revision smoke proved Maya call → evidence-backed CRM → grounded draft → audited approved-unsent state and re-read each artifact. Seventeen smoke tests, lint, build and six repository jobs pass. T1 still scores 5/10 on the honest keyless memory deployment; the surfaced misleading live-pipeline label is fixed to `Run verified API demo`. Other people should submit the form and supply managed credentials securely. |
| `local-reasoning-runtime` | Run genuine schema-constrained extraction and drafting on a loopback-only open-weight model when hosted-provider credentials are unavailable | `api/app/core/`, `api/tests/`, `deploy/local-model/`, `scripts/`, VPS runtime | Jeremy's agent | feat/local-reasoning-runtime | done | PRs #30 and #31 installed the official Apache-2.0 Qwen2.5 1.5B Instruct Q4_K_M GGUF behind loopback, pinned the image and model checksum, added model-native context admission and made `/ready` report the effective provider. Production stored genuine model extraction and draft artifacts; 307 API tests and four hostile review rounds passed. CPU inference takes minutes, so demo records are precomputed. Other people should submit the external form and must not expose port 8081. |
| `grounding-value-repair` | Preserve strict transcript grounding while giving the paused demo campaign a safe recipient from structured email envelope data | `scripts/` | Jeremy's agent | feat/grounding-value-repair | done | PR #33 kept malformed model evidence fail-closed after three hostile review rounds rejected transcript heuristics. The seeder independently proves local-model call extraction/drafting, then enrolls a grounded email reply whose validated sender supplies the recipient. Seven seed tests plus every repository CI job pass. Other people should submit the external form and must not resume the intentionally unsent campaign. |
| `local-draft-safety` | Prevent a small local model from echoing challenged guarantees into a follow-up and make cold production seeding recover safely after Node's five-minute header ceiling | `api/app/services/draft.py`, `api/app/prompts/`, `scripts/` | Jeremy's agent | feat/local-draft-safety | done | PR #35 merged at `b519e25`: risky local-model claims fail closed and ambiguous transport recovery accepts only an exact-model stored extraction within a strict deadline. All six repository jobs and the final hostile review passed. Production cold-seeded a safe approved-unsent model draft and a validated email reply into the 2099 paused campaign with one queued, zero attempts and zero sent. Other people should submit the external form and must not resume the demo campaign. |
| `local-embedding-runtime` | Run a pinned loopback-only embedding model so keyless production can derive a real aggregate ICP from loaded call and email evidence | `api/app/core/`, `api/tests/`, `deploy/local-model/`, VPS runtime | Jeremy's agent | feat/local-embedding-runtime | done | PR #40 installed the pinned Apache-2.0 Nomic Embed Text v1.5 Q4_K_M runtime with exact readiness and transactional deployment. Production reports local embeddings and a model-derived mixed-channel ICP. Other people should submit the external form and must not expose port 8082. |
| `local-provider-route-guard` | Let ICP derivation and lead outreach use the configured local reasoning provider without treating its provider alias as an integration-map key | `api/app/routers/{icp,leads}.py`, route tests | Jeremy's agent | fix/local-provider-route-guard | done | PR #41 fixed the production-discovered provider-key mismatch and covers both ICP and outreach routes. All six CI jobs and final hostile review passed. Other people should submit the external form. |
| `ground-icp-profile` | Keep the small local model's aggregate ICP focused on won-customer attributes and generate an actionable lead brief without seller-performance leakage | `api/app/services/icp.py`, ICP prompt and tests | Jeremy's agent | fix/ground-icp-profile | done | PR #42 validates and reconciles won-only industries, roles, triggers and headcount before constructing the customer summary and Origami brief. The exact production path proves 12 calls plus one email. Other people should submit the external form. |
| `ground-icp-evidence` | Replace seller-performance explanations in the model response with dimension-specific evidence that cites only supporting won deals | `api/app/services/icp.py`, ICP tests | Jeremy's agent | fix/ground-icp-evidence | done | PR #43 makes all four public evidence dimensions cite only the retained values and five supporting won deals. Production revision `c0a9b41` passed exact smoke with the campaign paused and zero sent. Other people should submit the external form. |
| `live-runtime-docs` | Reconcile judge-facing documentation with the deployed local-model, mixed-channel ICP and campaign evidence | `README.md`, `PROJECT.md`, `BOARD.md`, `STATUS.md`, `docs/submission-checklist.md` | Jeremy's agent | docs/live-local-runtime | done | PR #44 removes stale hosted-only and no-model claims, records exact live ICP/campaign evidence and updates the final handoff. Deterministic submission and smoke suites pass. Other people should submit the external form. |
| `icp-source-links` | Let live Derived ICP evidence chips open the cited deal's known Slipstream calls | `api/app/schemas/icp.py`, `api/app/routers/icp.py`, `components/intelligence/`, `lib/api/` | Jeremy's agent | feat/icp-source-links | done | PR #45 adds a backward-compatible, bounded cited-deal map and resolves it only to known call routes without interaction content. Three hostile review rounds are clean after fixes; 353 API tests, 32 smoke tests, lint, build and deterministic submission eval pass. Other people should submit the external form. |
| `revenue-loop-demo` | Turn the complete sales loop into one memorable, stage-ready guided replay with live proof | `app/(app)/demo/`, `components/demo/`, sidebar, demo script | Jeremy's agent | feat/revenue-loop-demo | done | PR #46 merged at `c16d791`; it separates the recorded Maya journey from independently refreshed runtime, exact-model, substantive ICP and exact-campaign proof and shows a clearly labelled 10 min × 8 calls = 6.7 hours/$500 weekly capacity illustration. Thirty-eight smoke tests, lint, production build, real Chromium checks and all six CI jobs pass. Other people should submit the external form. |
| `judge-value-proof` | Quantify the larger coaching and targeting upside without presenting assumptions as customer results | Revenue Loop, README value evidence, demo script, responsive shell | Jeremy's agent | feat/judge-value-proof | done | PR #56 adds computed what-if cards for one prevented miss and one point of better replies; Salesforce's 2026 research corroborates the broader admin problem; every number is labelled illustrative rather than forecast or pilot evidence. B1 scores 8/8 and B3 scores 9/9. Desktop and 390px browser renders pass. Other people should preserve the evidence labels and submit the external form. |
| `revenue-dna-shock-test` | Let judges safely watch a hypothetical CRM outcome propagate through the targeting loop without mutating production | Intelligence Revenue DNA, simulation helper, smoke contract, demo script | Jeremy's agent | feat/revenue-dna-shock-test | done | PR #57 uses the live profile version and lead count, then visibly stages signal → stale detection → re-score protection → next version. It is explicitly local, makes no provider call and leaves live state unchanged. I1 scores 8/10 and I2 scores 8/8; 43 smoke tests and a real browser click-through pass. Other people should preserve the non-mutating labels. |
| `two-key-backend` | Make the backend turnkey and safely testable by judges | `api/app/core/`, demo/provider routes, setup docs and tests | Jeremy's agent | feat/two-key-backend + fix/mixed-channel-demo-evidence | done | PRs #47 and #48 are merged and API revision `e6c2359` is live. One OpenRouter key powers verified `openai/gpt-5.4` extraction, mixed call-and-email ICP derivation, embeddings and batches of exactly ten safe fictional `.example` prospects; Origami is optional. 361 API tests, 38 smoke tests, build and a real Chromium run pass. Other people should submit the external form and avoid changing these backend integration surfaces. |
| `judge-live-evidence` | Make the tested live loop visible to server-rendered judges and expose a bounded machine-readable proof | `app/(app)/demo/`, `components/demo/`, `api/app/routers/demo.py`, `evals/`, production smoke | Jeremy's agent | feat/judge-live-evidence | done | PR #51 is merged; API revision `c342825` and Vercel deployment `dpl_35iFEtxJ18tBy7kF541UVzLh64HX` serve the dynamic first-response Revenue Loop, provider verification, mixed-channel ICP, fictional lead and exact Maya evidence. All 363 API tests, 40 smoke tests and six CI jobs pass, and strict T1 rose from 6/10 to 8/10. Other people should submit the external form and avoid restarting the VPS during this proof window. |
| `revenue-dna-freshness` | Detect when a new CRM outcome invalidates the current ICP, expose affected lead versions and block paid sourcing against stale evidence | `api/app/services/icp.py`, `api/app/services/leads.py`, `components/intelligence/`, `lib/api/` | Jeremy's agent | feat/revenue-dna-freshness | done | PR #53 merged at `16520b8`: server-owned cohort fingerprint, public freshness contract, OpenRouter reuse check, pre-provider stale gate and stage-visible Revenue DNA card. All 367 API tests, 41 smoke tests and six CI jobs pass; exact API and UI deployments are live with ten leads bound to the current version. Other people should submit the external form and avoid restarting the memory-backed API. |
| `campaign-revenue-dna-gate` | Carry the live targeting fingerprint into outreach execution so Campaigns proves the closed loop instead of looking like a generic sequence table | `app/(app)/campaigns/`, `components/campaigns/`, presentation docs | Jeremy's agent | feat/campaign-revenue-dna-gate | done | PR #69 reads the existing non-mutating freshness contract beside live campaign state, with independent loading/error handling and a direct evidence link. Exact web revision `a21e2ba` is live; desktop and 390px production renders have no document overflow, the evidence link resolves, all 50 smoke tests pass and the campaign remains paused with zero sends. The production-aware I2 judge scores 8/8 and cites this gate. Other people should preserve the source-versus-delivery boundary and avoid resuming the demo campaign. |
| `eleno-rebrand` | Rebrand the seller from Harbourline IT to Eleno: rewrite the 12 history calls and the voiced demo call for an AI and automation consultancy, add Eleno's seven public clients as won CRM deals, retune the draft safety vocabulary, re-derive the ICP and leads | `fixtures/`, `lib/data/`, `components/`, `api/tests/`, `api/app/services/draft.py`, `api/app/services/fixture_history.py`, `scripts/`, `docs/` | Anna | feat/eleno-fixtures | done | Jeremy agreed 13 Sep. Validator, 6 fixture tests, 371 API tests, lint, build, 52 smoke tests and a browser walk of the demo path pass against a production-like local API. After merge Jeremy redeploys API and web, then the production bootstrap, campaign seed and smoke run over HTTPS. |
| `model-coverage` | Keep the judge's coaching narratives on the scorecard instead of templated sentences, validated against the transcript, and render them on the run page | `api/app/services/score.py`, `api/tests/test_scorecard.py`, `lib/adapters.ts`, `lib/types.ts`, `components/RunTimeline.tsx`, `README.md`, `docs/architecture.md` | Anna | feat/model-coverage | done | From the 13 Sep AI-usage audit. PR #83. The email-reply half of the original claim landed in PR #81 (`email-reply-model`); the duplicate built here was dropped. Still open from the audit: speaker roles on uploaded audio are a string match in `transcript_from_segments` (Jeremy's `ingest` lane). |
| `call-coach` | Replace the desktop coach with the session-based call coach (microphone plus system audio, suggestions that retire when asked or answered), port its session API, and add a "Start call with coach" entry beside "Add a call" | `coach/`, `api/app/routers/coach_sessions.py`, `api/app/services/coach_*.py`, `api/app/prompts/coach-session-*.md`, `api/tests/test_coach_sessions.py`, `supabase/migrations/` (new coach migration only), `app/api/coach/`, `components/coach/`, `components/shell/Topbar.tsx`, `lib/coach/`, `.github/workflows/` (coach jobs only) | Romain | feat/call-coach-integration | done | Agreed with the team 13 Sep; supersedes the `coach-shell` and `coach-release` desktop app. Phase 1 is the live call on the new-customer path; phase 2 adds CRM history and playbook context. The legacy `/coach/live` API stays until phase 1 is live. Note (Jeremy's agent, 13 Sep): merged origin/main in; the launcher moved from the removed top bar into Max's sidebar, the review page is `/coach/[id]` on the new design tokens, and the coach proxy reads `SLIPSTREAM_INGEST_TOKEN` (falls back to `INGEST_TOKEN`). |
| `demo-leads-naming` | Generate the ten fictional prospects with plausible company and contact names from the ICP instead of `ICP Match NN` placeholders, keep reserved `.example` domains, no contact details and the synthetic flag, and show the fictional label in Leads | `api/app/services/demo_leads.py`, `api/app/prompts/demo-leads-*.md`, `api/tests/test_demo.py`, `components/leads/` | Anna | PR #81 | done | Names must never collide with the seven public Eleno clients or fixture companies. |
| `email-reply-model` | Draft email thread replies on the configured reasoning model with thread grounding and the follow-up safety check, keeping the deterministic template only as the keyless fallback | `api/app/services/email.py`, `api/app/routers/emails.py`, `api/app/prompts/email-reply-*.md`, `api/tests/test_emails.py` | Anna | PR #81 | done | Approval and delivery semantics unchanged. |
| `coach-risk-flags` | Make the legacy live coach flag rep pressure, overclaims and unverifiable claims as they happen, citing the rep turn, with a keyless deterministic fallback | `api/app/services/coach.py`, `api/app/ws/coach.py`, `api/app/prompts/coach-*.md`, `api/tests/test_coach.py` | Anna | PR #81 | done | Additive message field only; Romain's `call-coach` session files untouched. |
| `playbook-patterns` | Return judge-found winning patterns from `/playbook` instead of an empty list, add deterministic scorecard patterns, and render both on Intelligence | `api/app/services/score.py`, `api/app/prompts/playbook-*.md`, `api/app/schemas/scorecard.py` (additive), `api/tests/test_scorecard.py`, `lib/intelligence.ts`, `lib/api/slipstream.ts`, `components/intelligence/` | Anna | PR #81 | done | Schema change is additive only. |
| `demo-call-recordings` | Voice a set of labelled fixture calls (won, lost, stalled, no-show) plus the demo call as web-served recordings judges can play from Home's call picker and a call's page | `public/recordings/`, `lib/data/recordings.*`, `components/DropZone.tsx` (picker only), `components/RunView.tsx` header, `fixtures/` voicing script | Jeremy's agent | feat/demo-call-audio | in progress | PR #85 open, not merged: five playable recordings (ElevenLabs demo call, Kokoro-voiced won/stalled/lost/no-show) awaiting review |
| `openrouter-reasoning-budget` | Keep mandatory-reasoning OpenRouter models (Claude Fable 5.1) from spending the structured-output token budget on thinking, so extraction, scoring, drafts and ICP stop truncating into 502s | `api/app/core/config.py`, `api/app/core/llm.py`, `api/app/services/score.py` (OpenRouter request body only), `api/.env.example`, `api/tests/test_llm.py`, `api/tests/test_scorecard.py` | Jeremy's agent | fix/openrouter-reasoning-budget | in progress | Production switched to `anthropic/claude-fable-5.1` on 13 Sep. Touches one function in `score.py` that `model-coverage` does not. |
| `paste-run-full-loop` | Make a pasted or uploaded transcript run the whole loop: parse `Name (Company):` speaker labels so scoring can map roles, relearn Revenue DNA when the new CRM evidence makes the ICP stale, draft real outreach for the top leads, and let the campaign seed accept model-drafted email replies | `lib/ingest.ts`, `lib/useRun.ts`, `scripts/seed-demo-campaign.mjs`, `scripts/*.test.mjs` | Jeremy's agent | feat/paste-run-full-loop | in progress | Does not touch `components/DropZone.tsx` or recordings. |

## Blocked and parked

| Slug | Why | Unblocks when |
|---|---|---|
| `model-bakeoff` | Done for scorecard (11 models) and extraction (7 models, then Claude and gpt-5.4 reruns, then the Eleno cohort); consolidated record in `docs/model-bakeoff.md`. The risk-flag task never ran. Owner Anna. | The demo loop runs end to end and the prompts in `api/app/prompts/` exist |
| `draft-judge` | Pairwise blind judge for follow-up draft quality, part of the bake-off | After `model-bakeoff` |
| `one-thread-latest` | Port Maxim's later one-thread commits (Settings, shared frame spacing, Leads sheet columns, Intelligence as a finding, Revenue loop beats) onto main's API-wired app tree | `app/`, `components/`, `lib/` | Jeremy's agent (for Maxim) | feat/one-thread-latest | done | PR #80 ports Maxim's 15 one-thread commits after `f33dc26` (Settings, shared frame spacing, Leads sheet columns, Intelligence as a finding, Revenue loop beats with previews) onto the API-wired tree; all previews fed from the API, Anna's gateway token handling and Eleno data kept, no new deps. tsc, lint, build, 38 smoke tests and evals:dry pass; every route renders locally. |
