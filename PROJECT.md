# Slipstream: build plan

The pitch (problem, target user, differentiation, value, feasibility, model choices) lives in `README.md` because the judges read that. This file is the plan we build from: surfaces, architecture, data model, pipeline, setup, timeline. Rules are in `CLAUDE.md`; ownership is in `BOARD.md`.

The name is the drafting effect: sit in the low-pressure wake and go faster on less effort.

## State of play, 13 September

- Jeremy's agent is adding judge-visible value proof: two computed, explicitly hypothetical pipeline scenarios quantify the coaching and Revenue DNA upside; the README now corroborates the admin problem with Salesforce's 2026 research; and the shared shell collapses cleanly on phone-sized screens after a real browser check exposed the old layout squeeze. Other people should preserve the “scenario, not forecast” labels and avoid restarting the seeded memory-backed API.
- Jeremy's agent completed the judge-facing pitch polish: business feasibility rose from 7/8 to 8/8, demo flow rose from 7/8 to 8/8, clarity holds at 5/6 with the spoken script cut to 531 words, and innovation holds at 7/10 with the live Revenue DNA mechanism recognized. The remaining `origin/feat/one-thread` branch was reviewed: it is a separate-history static prototype that would delete the integrated backend and most product surfaces, so it is not mergeable; its strongest contained sticky-timeline interaction is already represented by the live conversation view's sticky CRM evidence and activity timeline. Other people should use the contribution form for new work and avoid restarting the live memory-backed API while the seeded demo is verified.
- Jeremy's agent completed `feat/revenue-dna-freshness` in PR #53: every derived ICP receives a server-owned fingerprint of the CRM evidence and outcomes that produced it; the public freshness contract exposes drift and affected lead versions; OpenRouter demo reuse requires the exact cohort; and paid sourcing fails before provider I/O when the target is stale. API revision `16520b8` is live and freshly seeded over 13 deals, 12 calls and two emails with ten current fictional leads; Vercel deployment `dpl_F17QVRsNEv2zMTVq7NXLyzq77xJV` serves the Revenue DNA card and server-rendered paused campaign. All 367 API tests and 41 smoke tests pass. Jeremy's agent is hardening the production verifier's hosted-model timeout after a healthy extraction crossed its old ten-second ceiling. Other people should submit the external form and avoid restarting the memory-backed API.
- Jeremy's agent completed `feat/judge-live-evidence` in PR #51: the strict T1 judge's no-JavaScript snapshot now receives direct provider verification plus a bounded, embedding-free API proof of the live 12-call/two-email ICP and ten safe fictional leads; the Revenue Loop also server-renders the current model, cohort and paused campaign on first response. API revision `c342825` and Vercel production deployment `dpl_35iFEtxJ18tBy7kF541UVzLh64HX` are live at the canonical URLs, the Maya model extraction and approved-unsent draft were reseeded, the strengthened production smoke passes, and T1 rose from 6/10 to its 8/10 target. All 363 API tests, 40 smoke tests, build and six CI jobs pass. Jeremy's agent also repaired the documented campaign reseed CLI, which previously exported a function without invoking it. Other people should submit the external form and avoid restarting the VPS while this memory-backed evidence is live.
- Jeremy's agent completed the OpenRouter-only backend proof in PRs #47 and #48: exact API revision `e6c2359` is live, provider readiness is verified without model spend, and one click loads 12 calls plus a canonical email, derives a grounded mixed-channel ICP, generates exactly ten visibly fictional `.example` prospects, embeds and scores them against won deals, and stores provider/model provenance. The production reliability override uses `openai/gpt-5.4`; 361 API tests, 38 smoke tests, build and a real Chromium run pass. Origami remains an optional real adapter rather than a demo dependency. Other people should submit the external form and avoid weakening the fictional-data or no-delivery guardrails.
- Jeremy's agent completed the stage-ready Revenue Loop in PR #46: the guided replay separates the recorded Maya story from independently verified production runtime, exact model, ICP cohort and exact paused-campaign guardrail; all seven beats deep-link to evidence and presenter state persists. Thirty-eight smoke tests, lint, production build, real Chromium checks and all six CI jobs pass. Other people should submit the external form and avoid weakening the live-versus-recorded evidence labels.
- Jeremy's agent completed `feat/icp-source-links` in PR #45: stored ICP responses now return a bounded, PII-light map from each cited immutable source deal to its company and call IDs, and the live Derived ICP evidence chips open only known Slipstream conversation routes. Three hostile review rounds resolved legacy compatibility, post-commit failure, selective-read, privacy, limit and route-resolution findings; 353 API tests, 32 smoke tests, lint, the 28-route build and deterministic submission eval pass. Other people should submit the external form.
- Jeremy's agent completed `docs/live-local-runtime` in PR #44: the README, board, status and submission handoff now match the exact deployed local-model, mixed-channel ICP and paused campaign evidence without restarting the stable memory-backed demo. Deterministic submission and smoke suites pass. Other people should submit the external form.
- Jeremy's agent completed the local aggregate-intelligence sequence in PRs #40–#43. A separately pinned Nomic Embed Text v1.5 runtime now joins Qwen on an isolated loopback-only network; readiness verifies exact aliases and real finite vectors; ICP and lead records stamp the effective embedding identity; and model drift is rejected before any Origami spend. The first production run exposed and PR #41 fixed the `local`/`local_model` route-guard mismatch. PRs #42 and #43 then constrained the small model's public customer fields and evidence to validated, retained won-deal values. Exact production revision `c0a9b41` stores an ICP over 13 CRM deals, 12 calls and one email with four evidence dimensions citing five won deals, while the 2099 campaign remains paused with one queued and zero sent. All six CI jobs and final hostile reviews passed. Other people should submit the external form and provide managed Supabase, Origami, Resend or Railway credentials only through deployment secrets.
- Jeremy's agent completed `feat/local-draft-safety` in PR #35: local-model follow-ups now fail closed on explicit guarantees, absolute security claims, promised insurance savings and competitor-certification claims, while qualified or unrelated language remains valid. The cold campaign seeder reuses only an exact-model stored extraction after Node's known transport disconnect, polls only a pending 404 within the model deadline, and propagates definitive failures. Exact main revision `b519e25` passed all six repository jobs and the final hostile review, is live on the VPS, and cold-seeded a safe model draft plus a recipient-valid email reply into the 2099 paused campaign with one queued, zero attempts and zero sent. Other people should submit the external form, must not resume the demo campaign, and should provide managed Supabase, Origami or delivery credentials only through deployment secrets.
- Jeremy's agent completed `feat/local-reasoning-runtime` in PRs #30 and #31: the keyless VPS now performs genuine schema-constrained call extraction and follow-up drafting with Qwen2.5 1.5B Instruct Q4_K_M through a private llama.cpp service. The official Apache-2.0 model, container digest, 1,117,320,736-byte artifact and SHA-256 are pinned; port 8081 is loopback-only; model-native token admission protects the 16,384-token context; and hosted-provider credentials still take priority. `/ready` truthfully reports `reasoning_provider: local`, the exact model alias and `integrations.local_model: true` while unrelated integrations remain false. A production call extraction and approved-unsent draft retain exact model provenance. CPU inference takes several minutes, so demo artifacts are precomputed rather than generated on stage. PR #33 preserved the strict transcript-grounding boundary after three hostile reviews rejected unsafe repair heuristics, and moved the paused synthetic campaign recipient to validated email-envelope data. Other people should submit the external form, apply managed Supabase credentials when available, and never expose the model port.
- Jeremy's agent completed `feat/production-primary-loop-smoke` in PR #29: after a fresh exact-revision deploy, the production smoke idempotently ingested Maya's canonical call, stored evidence-backed CRM extraction, generated a grounded draft, recorded audited approval, proved it remained unsent, and re-read all three artifacts through public GET routes. Those bounded runtime records now join judge evidence. Seventeen smoke tests, lint, the 28-route build, deterministic evals and all six repository jobs pass. The strict T1 re-check still scored 5/10 because production is explicitly keyless and memory-backed, but it identified and Jeremy's agent fixed the misleading `Run live pipeline` copy to say `Run verified API demo`. Other people should submit the external form and provide managed credentials only through an approved secret channel.
- Jeremy's agent investigated `infra/self-hosted-supabase` in PR #28 and rejected it during production verification: the pinned Supabase CLI identifies its stack as local-development-only, bound ports 54321–54324 to every interface, and used shared default credentials. The installer failed closed; Jeremy's agent immediately stopped and removed every container and volume, deleted the generated environment/drop-in, disabled the unit, verified all ports closed, restored the healthy API, and reverted PR #28 at `a55d064`. Durable production storage remains blocked on approved managed-Supabase credentials or a separately hardened self-hosted deployment. Other people should submit the external form and supply credentials only through an approved secret channel.
- Jeremy's agent completed `feat/demo-evidence-parity` in PR #27: the public call-13 UI now matches the canonical voiced Donnie Azoff/Jordan Belfort fixture, an executable parity test prevents drift, and judge evidence directly captures the scripted Maya opening plus deployed API readiness and paused campaign state. Fifteen smoke tests, lint, the 28-route build, deterministic evals, targeted F2 and all six repository jobs pass. T1's stricter live re-check scored 6/10 because the deployed API truthfully reports memory storage and no model, Supabase, Origami or delivery credentials; the unattended credential audit found none available on this machine. The green PR build was assigned to the production alias when Vercel's daily promotion cap blocked a new production deployment. Other people should submit the external form and supply deployment credentials only through an approved secret channel.
- Jeremy's agent completed `feat/eval-live-route-evidence` in PR #25: every rubric judge now receives bounded server-rendered evidence from the public Conversations, call-detail, Intelligence, Leads and Campaigns routes, and final verdict parsing tolerates brace-containing paths and pretty JSON. Ten smoke tests pass; targeted T1 rose from 7/10 to the 8/10 target and T3 passed instead of turning its maximum-quality assessment into a parser failure. All six repository jobs passed in run `34714623752`. Jeremy's agent then completed `feat/campaign-loading-boundary` in PR #26, replacing the full client fallback and its duplicate API reader with a static accessible skeleton; eleven smoke tests, lint, the 28-route build and all six repository jobs passed in run `34714806940`. Other people should submit the external form.
- Jeremy's agent completed `feat/demo-video` in PR #24: a public 4:30 narrated walkthrough now covers the verified Conversations, evidence-backed CRM, scorecard/draft, Intelligence, Leads and live paused Campaigns surfaces. The capture and exact narration are reproducible, the MP4 is published under GitHub release `demo-video-v1`, and `npm run evals:dry` now passes. All six repository jobs and Vercel passed in run `34713422603`. Other people may replace it with a human-presented cut and should submit the external form.
- Jeremy's Next.js 16 and React 19 app is live with a unified call-and-email feed, API-backed call and email workflows, aggregate analysis and ICP, an Origami-ready lead handoff, and clearly labelled evaluation fallbacks where production integrations are unavailable. Build, lint and production smoke tests pass.
- Fixtures are merged: twelve labelled history calls plus the voiced demo call, a validator and tests (see `fixtures/README.md`). The independently accessible UI is deployed from GitHub-connected `main` at https://slipstream-hackathon.vercel.app.
- Jeremy's agent shipped the database migrations, reviewed FastAPI foundation, call and email ingestion, evidence-backed CRM extraction, grounded follow-up drafting, CRM writeback, realtime coaching backend, all four web-wiring lanes, secure Electron coach, and the judged pitch and live demo scripts. Email threads are provider/mailbox scoped, atomically populate the CRM, preserve curated fields, and create safe versioned reply drafts. Jeremy's agent also integrated and hardened Anna's scorecard lane: bounded non-blocking judge calls, transcript-grounded evidence, won-versus-not-won playbooks, canonical-source and revision-safe conversation persistence, and honest per-attempt eval accounting. The API is live at https://slipstream-api.3-104-149-193.sslip.io and both call and email ingest-to-approved-draft loops have been exercised in production. Other people should record and publish the submission video; Anna owns ICP/leads/outreach.
- Still missing from production: the eight later Supabase migrations, service credentials on the VPS, a real Origami run, and mixed call-audio capture. The VPS deliberately retains deterministic memory/fixture paths, and the UI labels evaluation data and unsent approvals rather than presenting them as live integrations. A public fallback demo video is now published; only the external submission form still requires a teammate.
- Jeremy's agent merged Max's Clay-style UI and the review-clean live conversation pipeline, then deployed it at https://slipstream-hackathon.vercel.app. The production browser now reaches the VPS for fixture ingest, evidence-backed CRM extraction, grounded draft creation and audited approval that remains truthfully unsent; the scorecard is explicitly labelled fixture data until a model key is installed. The public 4:30 fallback walkthrough now closes the deterministic video check; a teammate should submit the external form.
- Jeremy's agent fixed the VPS CORS allow-list for the real Vercel origin and added an explicit live conversation pipeline to Max's UI snapshot: fixture ingest, evidence-backed CRM extraction, grounded draft creation and approval now call the deployed API, while clearly-labelled fixture data remains the fallback. Other people should not replace `lib/api/slipstream.ts` or the conversation-detail pipeline state while this branch is in progress.
- Jeremy's agent completed `feat/web-wire-analysis`: the Intelligence surface validates and loads a stored live ICP and Origami brief when available, while every retained aggregate and unmatched evidence link stays explicitly labelled as the 12-call evaluation. Loading, API failure, missing-model, zero-call and changing-input states preserve that provenance. The coach overlay has since shipped; other people should record the public demo video and avoid weakening the live-versus-evaluation labels.
- Jeremy's agent completed `feat/web-wire-leads`: the UI now validates and loads stored leads, launches and polls bounded Origami jobs, preserves unknown scores, verifies ICP provenance, drafts outreach through the backend, and binds approval to the exact reviewed draft. Evaluation approvals stay local; backend approvals remain truthfully unsent until the authenticated delivery action. The API serializes redraft/approval per lead and rejects stale draft IDs with 409; its concurrency test passed ten repeated runs. The coach overlay has since shipped; other people should record the public demo video.
- Anna's Supabase project `slipstream` (ref `ozfogqrawonkluckhksk`, region ap-southeast-2) carries the initial schema, recorded in migration history via the CLI; the seed was not applied because its synthetic deals would join the fixture history. The ICP lane ran on that store on 12 September: 13 fixture deals loaded, 12 embedded, ICP version 1 persisted and read back after an API restart. Production deliberately remains memory-backed, but PRs #47 and #48 have since made the call/email aggregate, ICP, ten fictional leads and outreach proof executable with OpenRouter alone. Durable storage still requires the remaining managed migrations; a real Origami search is optional and unclaimed.
- Live state checked by Anna on 12 September, 21:00 AEST: the initial Supabase migration is applied (CRM, ICP and lead tables present) while the email ingestion and scorecard persistence migrations are not (see the `schema` row on `BOARD.md`), and the VPS API at revision 6b79e6c still reports storage `memory` and every integration flag false, so nothing live uses the database or a model key yet. The `ORIGAMI_API_KEY` line in the shared `.env` is a placeholder comment, not a key; leads stays blocked until a real key lands. The team's working keys are OpenRouter, Supabase and ElevenLabs; the API falls back to OpenRouter for reasoning and embeddings when the native OpenAI or Anthropic key is absent, and the ICP derive has been run end to end on that key alone. Two coach tests in `api/tests/test_coach.py` fail whenever `OPENAI_API_KEY` is present in the shell, on main as well as on branches, because the coach test setup does not clear provider environment variables the way `api/tests/conftest.py` does.
- Jeremy's agent completed `feat/coach-shell`: a GPL-preserving, transparent Electron sales coach with Gemini and hidden-capture behaviour removed. It has a credential-free manual demo and a consented rep-microphone mode using a server-issued ElevenLabs Scribe token, with strict IPC/navigation/permission boundaries and bounded, cancellation-safe session handling. Nine tests, the production WebSocket smoke, production dependency audit, Linux package and `.deb` build pass; the final hostile review is clean. Other people should record and upload the 3-to-5-minute submission video; its URL remains the only deterministic submission failure.
- Jeremy's agent is working on `feat/submission-readiness`: all 176 API tests and Ruff pass even with fake provider, Supabase and ElevenLabs credentials injected into the developer shell, closing the environment-sensitive coach-test failure Anna found. The README, pitch, architecture, demo script and submission checklist now describe the deployed memory store, explicit scorecard/playbook actions, provider-dependent transcription and labelled evaluation fallbacks without implying unavailable keys are installed. Other people should supply only the public demo-video URL and avoid adding secrets to the repository; Jeremy's agent is continuing the submission audit.
- Jeremy's agent deployed backend revision `f540be0` to the VPS and verified exact-revision readiness. A root-anchored `.vercelignore` now keeps desktop package artifacts out of the web upload; current `main` (`77d909f`) is live on Vercel, including the email workflow. Other people should keep generated coach installers out of web deployment inputs.
- Jeremy's agent completed `feat/web-wire-emails`: two labelled email threads now sit beside calls in the unified feed and execute the deployed provider-neutral ingest, ordered history read, grounded reply draft and exact draft approval. Live API facts and labelled account context remain explicitly separate. The UI now distinguishes approved-unsent from a provider-confirmed sent state; lint, build and final hostile review are clean. Other people should record the public demo video.
- Jeremy's agent completed `feat/web-wire-scorecards`: after the live call pipeline, scoring is an explicit action that proves the exact transcript snapshot before showing a live result. Intelligence discovers stored revision-pinned scorecards and derives a playbook only on an explicit click; both transcript and immutable score-result revisions are checked before model spend and repeated in the response. Other analysis sections stay separately labelled evaluation data. All 174 API tests, lint, build and the fifth hostile review pass. Other people should record the public demo video.
- Jeremy's agent completed `feat/coach-release`: pinned, least-privilege GitHub Actions test and package unsigned Linux, macOS and Windows coach installers from a manual run or version tag, with no signing credentials in the repository. Exact main revision `d876e5c` passed all three Node-24 hosted runners and uploaded 99 MB Linux, 266 MB macOS and 483 MB Windows artifacts in run `34694011356`; local Linux packaging and the final hostile review are also clean. Other people should record the public demo video.
- Jeremy's agent completed `feat/ci`: credential-free GitHub checks protect the web build, API lint/tests, fixture validation/tests and coach tests on every pull request and main push using committed lockfiles and immutable action pins. Exact main revision `d876e5c` passed all four clean-runner jobs without annotations in run `34694007012`; the workflow and all exact local commands passed a hostile review. Other people should record the public demo video and avoid adding provider secrets to CI.
- Jeremy's agent completed `feat/playbook-store`: aggregate playbooks receive a deterministic cohort revision, persist through a bounded memory or Supabase repository path, and hydrate the Intelligence lens from the newest still-current cohort. Post-judge source changes return 409; the Supabase RPC locks and revalidates every scorecard before an atomic timestamp-ordered upsert. API revision `70d1cbc` and Vercel deployment `dpl_4To7zaLhVNc4nrUpXm4vtrK6SFSx` are live; 176 tests, build and the final hostile review are clean. Other people should record the public demo video.
- Jeremy's agent completed `feat/database-ci`: an ephemeral Supabase job applies every migration and runs all pgTAP suites without hosted credentials on every pull request and main push. Its first run caught a pre-existing `public.digest` namespace bug in the unapplied scorecard migration; changing it to Supabase's `extensions.digest` made all four migrations and all three database suites pass in run `34695291147`. PR #8 is merged. Other people should record the public demo video.
- Jeremy's agent completed `feat/unified-icp-evidence` in PR #9: ICP derivation now uses a private, bounded snapshot of outcome-labelled CRM deals enriched with call and email evidence while the twelve-call fixture benchmark remains unchanged. New profiles preserve immutable won-deal snapshots for stable lead similarity; memory mirroring preserves curated CRM fields; and Intelligence exposes exact call, email, deal and outcome-labelled counts. All 182 API tests, the web build, five fresh migrations and four pgTAP suites passed in run `34696878908`; the seventh hostile review was clean. Backend revision `6e2cfc4` and Vercel deployment `dpl_FDnRurKCiEDE1bu1RNUbtwLx6tTm` are live. Other people should record the public demo video and apply the four pending hosted Supabase migrations when credentials are available.
- Jeremy's agent completed `feat/icp-evidence-inventory` in PR #10: `GET /api/v1/icp/evidence` exposes the exact bounded call-and-email cohort available to the next ICP run without needing model credentials. Intelligence separates sufficient evidence from provider readiness and labels fixture-capable stored evidence without claiming live CRM provenance. All 184 API tests, the web build, five migrations and four pgTAP suites passed in run `34697558211`; the third hostile review was clean. Backend revision `e5aa1a4` is live and returned a truthful empty memory-store cohort in production. Vercel rejected both its GitHub preview and the manual production attempt after the project exceeded the free plan's 100-deploy daily limit, so the prior healthy production UI remains served until that external limit resets. Other people should record the public demo video.
- Jeremy's agent completed `feat/origami-v3-contract` in PR #11: the adapter now follows Origami's published v3 search, job and row-list contract, including quality forwarding, repeated-ID queries, bounded cursor pagination, typed cells, `needs_input`, `Retry-After`, strict overall polling deadlines and truthful live provenance. Provider-controlled results are bounded to the requested 100 leads and CSV fallback is scoped to those exact row IDs. All 201 API tests, the web build, five migrations and four pgTAP suites passed in run `34699477775`; both final hostile reviews returned `CLEAN`. Backend revision `c6230fb` is live and the Vercel deployment passed. No Origami key or credits were available, so a live search remains explicitly unclaimed. Other people should record the public demo video and provide any real key only through deployment secrets.
- Jeremy's agent completed `feat/crm-webhook-sync` in PR #12: an authorised server-side action now delivers the evidence-backed call extraction to any existing CRM through a versioned webhook. The exact payload and retry identity are HMAC-authenticated; transcripts and evidence quotes stay inside Slipstream; corrected payloads receive new identities; admission and duration are bounded; and ambiguous network outcomes are reported for reconciliation instead of claimed as failures. All 218 API tests, the web build, five migrations and four pgTAP suites passed in run `34700433859`; the third hostile review returned `CLEAN` after one high and four medium findings were fixed. Backend revision `7f0f360` and its OpenAPI route are live; `/ready` truthfully reports `crm_webhook: false` until deployment-only receiver secrets exist. Other people should record the public demo video and install receiver secrets only in deployment configuration.
- Jeremy's agent completed `feat/email-delivery` in PR #13: approval now records an audited `approved` state without claiming delivery, while `POST /api/v1/drafts/{id}/deliver` sends only the exact stored copy through Resend behind the ingest token. A production-only Supabase requirement, durable pre-send ledger, sender-and-content-bound idempotency key, 23-hour-55-minute ambiguity cutoff, bounded provider/database concurrency and atomic completion prevent duplicate or falsely reported sends; outreach approval and delivery preserve lead lifecycle in both stores. Historical simulated statuses are migrated back to `approved`. All 244 API tests, the web build, six migrations and five pgTAP suites passed in run `34702933115`; the fifth hostile review returned `CLEAN` after 3 high, 12 medium and 2 low findings were fixed. Backend revision `4bd5000` and Vercel are live, with approval smoke-tested as `approved`/`sent_at:null` and the unconfigured delivery route failing safely. Other people should record the public demo video and apply the seven pending hosted Supabase migrations when credentials are available.
- Jeremy's agent completed `feat/batch-delivery` in PR #14: a trusted campaign service or Railway/Marcel scheduler can submit up to eight explicit approved draft IDs and receive ordered per-item send, retry and reconciliation outcomes without putting the delivery token in browser code. Shared single/batch admission, absolute deadlines, disconnect-aware cancellation and exact idempotency identities bound provider risk; a migration-first compatible Supabase claim/start ledger distinguishes unsubmitted reservations from ambiguous sends and freezes delivery-sensitive content once armed. All 277 API tests, the web build, seven migrations and five pgTAP suites passed in run `34707606363`; backend revision `12b7fba` is live, exposes the batch route, rejects unauthorised calls before configuration disclosure and truthfully reports email delivery unconfigured. Other people should keep the campaign UI labelled as evaluation data until it is backed by durable campaign records, and should record the public demo video.
- Jeremy's agent completed `feat/campaign-orchestration` in PR #15: explicit approved-draft membership persists in memory or Postgres; a trusted Railway/Marcel scheduler claims at most eight due items under a fenced lease; and exact delivery outcomes resume as scheduled, sent, retryable, failed or reconciliation-required. Campaign creation is idempotent against immutable enrollment data, browser-safe reads need no delivery token, and the provider configuration is checked before work is claimed. All 287 API tests, eight migrations, six pgTAP suites and seven CI checks passed after the hostile review found and prompted fixes for JSON-null receipts, retry identity, store parity and executor cleanup. Backend revision `3712890` is live and exposes all three campaign routes; production remains truthfully empty and unsendable until deployment-only Supabase, Resend and scheduler credentials exist. Other people should record the public demo video.
- Jeremy's agent completed `feat/web-wire-campaign-status` in PR #16: the existing campaign workspace now has a separate live-execution view sourced from browser-safe campaign reads, including confirmed sends, queued work, retryable failures, reconciliation-required items, safe provider receipt identifiers and attempt counts. Strict response validation, abort-and-request fencing, persistent expansion state and live-region announcements make refreshes robust; the sequence designer remains explicitly labelled evaluation data and no scheduler token enters browser code. All five repository CI jobs passed in run `34709284276` and two hostile reviews were resolved. Production deployment `dpl_BYqy6vsD3jnBTxRJ8ZRiJfHW9wEX` is healthy and the canonical Campaigns page exposes “Delivery execution”. Other people should record the public demo video.
- Jeremy's agent completed `feat/campaign-controls` in PR #17: authenticated, idempotent pause and resume actions stop future campaign claims without changing exact membership, reject an in-flight chunk rather than pretending to cancel provider I/O, and resume only unfinished queued or retryable work at its preserved schedule. Memory and Postgres implementations match, the RPC is service-role-only, and all 289 API tests, nine migrations, seven pgTAP suites and seven CI checks passed with a clean hostile review. Backend revision `e92c87b` is live and its OpenAPI controls reject unauthenticated production calls before disclosing state. Other people should record the public demo video.
- Jeremy's agent completed `feat/scheduler-worker` in PR #18: a standard-library-only, non-root container now invokes one bounded due-campaign chunk and exits for Railway cron. It rejects non-HTTPS URLs, embedded credentials, redirects, hostile headers and malformed/oversized responses; a wall-clock alarm defeats trickle responses, SIGTERM stops PID 1 cleanly, logs contain only a PII-free summary, and retry remains with the durable lease/ledger. Railway's current config schema and five-minute minimum were verified against its official documentation. Seven worker tests and all six existing CI jobs passed after three hostile reviews. Exact main revision `1a75a1b` is merged; deployment could not proceed because the installed Railway CLI has no authenticated account, so only deployment variables remain. Other people should record the public demo video.
- Jeremy's agent completed `feat/campaign-demo-story` in PR #19: the 602-word pitch, timed demo, README and judge Q&A now show explicit enrollment, server-side scheduled batches, pause/resume, durable outcomes and the honest keyless boundary without claiming the public deployment sent an email. The live demo includes the campaign beat only when Vercel has promoted it, and otherwise skips it cleanly. All six CI jobs passed; deterministic submission checks fail only on the human-owned demo-video URL. Other people should record and publish that video, then rerun the final checklist.
- Jeremy's agent completed `feat/production-smoke` in PR #20: `npm run smoke:production` now performs one credential-free, bounded verification of the live Campaigns marker, exact API revision, public campaign reads, every campaign HTTP operation and rejected unauthenticated controls. Three offline contract tests run in the web CI job; all six jobs passed in run `34711088144`, and the real production check passed against the canonical URLs. Other people should record and publish the public demo video, then run this gate with the final main SHA.
- Jeremy's agent completed `feat/demo-campaign-seed` in PR #21: the API now has a deployment-only random ingest token and the keyless memory deployment exposes a real synthetic campaign paused until explicitly resumed, scheduled for 2099 with one queued, zero sent and zero attempts. The idempotent HTTPS-only seed command recreates the fixture call, grounded approved draft and campaign after a restart, but refuses durable storage or configured email delivery. Six script tests and all six CI jobs passed in run `34711598467`. Other people should record the public demo video and must not resume the “Hackathon demo — intentionally unsent” campaign.
- Jeremy's agent completed `feat/extraction-eval-evidence` in PR #22: Anna's completed seven-model, thirteen-call extraction bake-off is now preserved on current main without pulling obsolete implementation code across twenty subsequent merges. Mistral Medium 3.1 is the operational pick at 86.5% mean judged pass rate, 7.4-second mean latency and $0.0034 measured cost per call; the next-step and objection-handling gates remain explicitly failed, and unknown direct-provider cost is not presented as zero. All six CI jobs passed in run `34712070620`; superseded PR #7 was closed with a handoff comment. The public GitHub repository now also has a judge-facing description, production homepage and eight relevant discovery topics. Other people should record the public demo video.
- Jeremy's agent completed `feat/evidence-sequence-repair` in PR #23: raw model evidence may omit a segment index long enough for the deterministic quote matcher to locate and repair it, but canonical extraction results still reject every unindexed transcript span. This closes the shared schema failure behind five Claude Haiku calls and one DeepSeek call in Anna's bake-off; the historical scores remain unchanged until a credentialed rerun. All 292 API tests and all six repository CI jobs passed. Other people should record the public demo video.
- The mock data in the prototype is the target shape for the API. Whoever claims a wiring row replaces the mock arrays with Supabase reads and API calls without changing the information architecture unless the chat agrees.

## The demo loop

The call and email writeback loops are executable from the production UI. Analysis and
lead surfaces use validated API responses when integrations are available and visibly
labelled evaluation fallbacks otherwise. The video must only claim a step is live after
it has been exercised on the deployed UI.

1. **A sales call happens.** One demo call, synthesised with ElevenLabs text-to-dialogue and played through speakers. Twelve further scripted calls (won, stalled, lost, no-show) are seeded as text-only CRM history so the analysis and ICP steps have something real to work from.
2. **The coach listens.** The Electron overlay can stream the consented rep microphone to Scribe realtime; the configured reasoning model returns the next questions to ask, grounded in the deal's CRM history. Its credential-free manual mode demonstrates both speakers without claiming mixed-audio capture.
3. **The call writes itself into the CRM.** Scribe batch transcribes with diarisation; the configured reasoning model extracts contact, company, deal stage, promises, objections and next step into our CRM tables, shown to the rep for approval. The fixture path is the production fallback when provider keys are absent.
4. **The follow-up drafts itself.** A draft email attaches to the deal. Approval is one click and audited without claiming a send. The separately authenticated delivery endpoint can send the exact approved copy through Resend when credentials are installed.
5. **The team learns.** Scorecard per call against a written rubric; aggregate lens on what correlates with won deals; ICP derived from won-deal embeddings.
6. **The ICP finds the next customer.** The ICP becomes an Origami brief; leads return, are scored against the won-deal profile, and each gets a one-click outreach draft.

## Surfaces

### Conversations (`app/`)

The unified feed of calls and emails. A call opens to the diarised transcript, participants, deal, outcome, extracted CRM fields, scorecard and follow-up draft. The provider-neutral email backend and production UI ingest inbound and outbound threads, read ordered history, draft grounded replies and approve the exact reviewed draft. Approval is an audited unsent state; the server-side Resend adapter is the only path that can record delivery.

### Analysis (`app/analysis`)

- **Sales training lens.** Per-call scorecards and the aggregate: which behaviours correlate with won deals and which with stalls.
- **ICP discovery.** The profile of the buyer who actually converts (industry, size, role, trigger), derived from won deals, with the evidence behind each attribute.
- **Outreach on that ICP.** The derived ICP as an editable Origami brief, the leads it returned, their similarity score, and the outreach draft per lead.

### Leads (`app/leads`)

Company, person, title, email, LinkedIn where Origami returns it, Origami relevance score, our similarity score against won deals, status. Each row has an outreach draft and an approve button.

### Coach (`coach/`)

Translucent always-on-top window adapted from Cheating Daddy under GPL-3.0, with its Gemini, screenshot, hidden-window and interview-answering behaviour removed. In demo mode, the rep commits both sides of a transcript manually with no provider credentials. In live mode, the official ElevenLabs client captures the consented rep microphone using a server-issued single-use Scribe token and sends committed text to the Slipstream coaching WebSocket. Capturing both sides requires a mixed call-audio source and is not claimed by this build. The API returns short grounded next-move suggestions and persists the call on confirmed completion.

## Architecture

```
app/          Next.js UI on Vercel. Uses the API for live reads and actions, with labelled evaluation fallbacks.
api/          FastAPI, Python 3.12, uv. Owns the AI pipeline. REST plus one WebSocket at /ws/coach. Deploys to Jeremy's VPS behind HTTPS.
coach/        Electron overlay. Talks only to the API WebSocket.
fixtures/     Thirteen call scripts with expected extraction and scorecard labels; audio for the demo call only. Loaded through the real ingest path.
supabase/     Migrations (pgvector enabled) and seed.
```

**Boundaries.** Supabase Postgres is the durable production store design and the API owns access; the VPS currently uses the same repository interface in memory until its Supabase credentials and remaining migrations are installed. A loopback-only llama.cpp service supplies credential-free reasoning but not embeddings. No queue or Redis. Long jobs (transcription, Origami polling) run as FastAPI background tasks that write status rows the UI polls.

**Data flow for one call.** Audio file in, Scribe batch (diarised), transcript row, model extraction (structured output, pinned schema, grounded quotes), contact, company, deal, note and task rows, model scorecard against the rubric, embedding of the call summary stored on the deal, follow-up draft row. Aggregations and ICP derivation read the deal embeddings.

**Coach flow.** The overlay either accepts manual rep/prospect turns or sends consented rep-microphone audio directly to ElevenLabs Scribe using a single-use token issued by the API. Committed transcript turns go to the API coaching WebSocket, which keeps a bounded rolling transcript and returns grounded next-move suggestions. On End & save, the same WebSocket persists the canonical call; the UI only claims success after the API sends `completed`.

## Data model

CRM tables mirror HubSpot objects so the path to a real integration is a field mapping, not a redesign.

| Table | HubSpot object | Notes |
|---|---|---|
| `contacts` | Contact | name, email, title, phone, company_id |
| `companies` | Company | name, domain, industry, size_band, location |
| `deals` | Deal | stage, amount, outcome (won, lost, stalled, open), summary, embedding vector |
| `calls` | Call (engagement) | audio_url, transcript, diarised segments, duration, outcome, deal_id, scorecard JSON |
| `notes` | Note | extracted promises, objections, next step; linked to deal and call |
| `tasks` | Task | agreed next steps with due dates |
| `drafts` | Email (engagement) | follow-up and outreach drafts, status draft or approved |
| `icp_profiles` | none | derived profile, evidence, the Origami brief text, version |
| `leads` | Contact (lifecycle lead) | Origami row id, fields, relevance_score, similarity, status |
| `activities` | Timeline | append-only log of everything Slipstream did to a record |

## AI pipeline

The reasoning model is not fixed: `REASONING_MODEL` selects it, and one `structured()` helper in `api/app/core/llm.py` routes to Anthropic, OpenAI, OpenRouter or the loopback local model. Hosted credentials take priority; without them, production uses the explicitly named Qwen2.5 local runtime. When the native key is absent and `OPENROUTER_API_KEY` is set, the same hosted model is routed through OpenRouter under its vendor-prefixed id (`gpt-5.4` becomes `openai/gpt-5.4`). The bake-off in `docs/model-bakeoff.md` records the accuracy/latency choice. Embeddings remain OpenAI `text-embedding-3-small` (1536 dimensions, matching the schema), called directly or through OpenRouter; the local model does not pretend to provide embeddings. Implementation notes:

- **Extraction** runs through `structured()` on the configured model for every call, fixture calls included, with a pinned JSON schema in `api/app/schemas/`. Every field has a confidence and a transcript span so the approval UI can show where a value came from. A deterministic grounding pass then checks each quoted span against the transcript: a paraphrased or mis-cited quote is rewritten to the exact segment text, and a claim whose quote matches nowhere is dropped (a field becomes null, a promise or objection is removed), with the counts recorded on the result. Copying the fixture labels happens only when no reasoning provider is configured, and the result says so (`source: fixture_labels`). The prompt receives the call date so partial dates such as "Thursday" resolve to the right year.
- **Follow-up draft** is written by the same model from the extracted fields, the transcript and the seller profile (`api/app/prompts/follow-up-v1.md`), signed by the rep who owns the next step; a body over 220 words is rejected. The string template is the no-provider fallback. Approval logs review but remains unsent. Credential-gated delivery sends the exact stored copy with a content-bound idempotency key and records the provider receipt.
- **Scorecard** rubric lives in `api/evals/rubric.md`: discovery questions asked, next step secured, objection handled, talk ratio. LLM-as-judge returns a score and a quoted span per dimension.
- **Eval** in `api/evals/`: twelve history calls hand-labelled for the four dimensions and the extraction fields, one script that reports agreement. This is the artefact for the "Use of Data / Models" criterion.
- **ICP derivation**: embed won-deal summaries with OpenAI `text-embedding-3-small`, its OpenRouter route, or the pinned local Nomic fallback; cluster them; have the configured reasoning model name the profile and cite the deals behind each attribute; then render the Origami brief from the profile. Lead scoring is cosine similarity to the won-deal centroid plus Origami's own relevance score.
- **Coach** prompt gets the deal context and the last 60 seconds of transcript; returns at most three questions and any detected commitment.
- **Prompts** are versioned files in `api/app/prompts/`. Transcript text is data; instructions inside a transcript or an Origami row are never followed.
- **History for the ICP** is loaded from the fixture labels (`POST /icp/history/load`) and tagged `metadata.source = "fixtures"`; derivation reads that CRM history plus eligible live mirrored deals, so its provenance counts match the evidence the judges can inspect. The current production run uses OpenRouter `openai/gpt-5.4` and `text-embedding-3-small` over 13 deals, 12 calls and two emails; the second email belongs to the paused provider-neutral campaign. An earlier hosted run on the Supabase store (project `ozfogqrawonkluckhksk`, initial schema applied) persisted and read back version 1 after an API restart. PostgREST returns pgvector columns as text, so the store parses `embedding` back into a list before validating deal and lead rows.

## External services and setup

None of these accounts exist yet. All go on personal accounts, not company billing.

| Service | Needed for | Action |
|---|---|---|
| Supabase | The database, pgvector | Done on Anna's `slipstream` project (schema applied 12 September, history recorded with `supabase migration repair`). Link with `supabase link --project-ref ozfogqrawonkluckhksk`; `supabase/.temp` stays untracked |
| Anthropic API | All reasoning | Key with enough credit for the weekend |
| ElevenLabs | Text-to-dialogue, Scribe batch, Scribe realtime | Check the plan covers realtime concurrency |
| Origami | Lead discovery | Paid plan, key from Settings, Developers. Start every search at `count: 10` and use fetch-more; credits are spent per row |
| Vercel | UI hosting | Connect the repo, set env vars |
| Jeremy's VPS | API hosting | Deploy script under `api/`, Caddy or nginx with a certificate in front of uvicorn, env vars from `.env.example` |

**Origami v3 essentials.** Base URL `https://origami.chat/api/v3`, header `Authorization: Bearer og_live_...`. `POST /leads/searches` with `{ "brief": "...", "count": 10, "quality": "fast" }` and one idempotency key returns a Job. Poll `GET /jobs/{job_id}` while honouring `next_poll_at` and `Retry-After`; stop safely on `needs_input`, `failed` or `cancelled`. `succeeded` gives `result.list_id` and `result.row_ids`. Read the exact result rows with repeated `ids` query parameters in pages of at most 100; the response envelope is `{ "object": "list", "items": [...], "next_cursor": null, "url": "..." }`. Cells may use typed `scalar` or `value` envelopes; sequences and malformed structured values are not coerced into CRM fields. CSV fallback is requested only for the same exact IDs, never the full list. `POST /leads/lists/{list_id}/fetch` takes the same brief plus `quality: fast | accurate`. Contract source: `https://raw.githubusercontent.com/Origami-Agents/mintlify-docs/main/openapi-v3.yaml`.

**ElevenLabs essentials.** Text-to-dialogue: `POST /v1/text-to-dialogue`, `inputs: [{ text, voice_id }]`, model `eleven_v3`, keep each request under 2,000 characters, up to 10 voices. Batch STT: `POST /v1/speech-to-text`, `model_id: scribe_v2`, `diarize: true`, response has `words[]` with `speaker_id`, `start`, `end`. Realtime: Scribe v2 Realtime over WebSocket, see the client-side streaming guide in the ElevenLabs docs.

## Out of scope this weekend

- Phone system or dialler integration (Twilio, Aircall). Audio arrives as a file or through the coach.
- Writing to a real HubSpot. The object mapping above is the path.
- Installing an email-provider credential in the public demo. The adapter exists and fails closed while unconfigured; approval remains truthfully unsent.
- Auth, multi-tenancy, billing.
- A live Gmail or Outlook OAuth connector. The provider-neutral webhook contract and full email pipeline are implemented.
- A trained call-scoring model.

## Timeline

- **Saturday 12 September:** foundation rows claimed and pushed (schema, fixtures, API skeleton), each lane has a walking skeleton by tonight, Vercel and VPS deploys exist even if thin.
- **Sunday 13 September:** lanes converge on the demo loop on the live URLs, eval run, `docs/pitch.md` and `docs/demo-script.md` written, video recorded (a person walks and talks, cut to a screen walkthrough per feature).
- **Monday 14 September, before 12:00pm:** final deploy, README lines filled (`Production URL`, `Demo video`, `Track`), `npm run evals:dry` green, submission with track named.

## Open questions

- Which Supabase project the VPS points at. Anna's `slipstream` project has the schema and a persisted ICP; Jeremy's VPS reports memory storage. Pick one before the demo so history load, ICP and leads survive a restart.
- Does Cheating Daddy's macOS system-audio capture work on current macOS? If not, the coach uses microphone input and the demo plays the mock call through speakers.
- A live Origami response sample should be archived after the first credentialed ten-lead run to confirm the implemented published contract against production without storing prospect PII in the repository.
